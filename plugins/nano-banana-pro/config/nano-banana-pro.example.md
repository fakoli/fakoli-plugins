---
# Legacy project settings: .claude/nano-banana-pro.local.md
# Prefer user defaults created by `nanobanana.py config --init`.
# Keep GEMINI_API_KEY in the environment or ~/.env, outside the plugin cache.
default_model: "pro"
default_aspect: "1:1"
default_size: ""
output_dir: "./.nanobanana/out"
max_remix_images: 2
---

# Nano Banana Pro legacy settings

This format remains readable for existing installations. New settings use
`${XDG_CONFIG_HOME:-~/.config}/nano-banana-pro/config.json`.
