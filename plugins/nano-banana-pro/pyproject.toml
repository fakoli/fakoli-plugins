[project]
name = "nano-banana-pro"
version = "1.4.0"
description = "Portable CLI for Gemini image generation and local optimization"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0,<2",
    "Pillow>=10,<13",
    "PyYAML>=6,<7",
]
