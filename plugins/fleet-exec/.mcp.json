{
  "mcpServers": {
    "fleet-exec": {
      "command": "uv",
      "args": ["run", "--directory", "${CLAUDE_PLUGIN_ROOT}", "python", "-m", "fleet_exec"],
      "env": {
        "FLEET_EXEC_TIMEOUT": "30",
        "FLEET_EXEC_MAX_BYTES": "256000"
      }
    }
  }
}
