---
description: Analyze a Claude Code or Codex session and produce a retro report
allowed-tools: Bash, Read, Write
argument-hint: [session selector or log path]
---

# /session-retro

Use the `session-retro` skill to locate the requested session, generate the deterministic stats/report/site artifacts, and write them to the selected `post-session-findings` destination.
