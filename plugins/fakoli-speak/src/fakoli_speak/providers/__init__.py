"""TTS provider implementations package.

Provider modules live here and call ``registry.register()`` at import time.
Discovery is handled automatically by :func:`fakoli_speak.registry.discover_providers`.
"""
