---
description: Validate plugin metadata, paths and hook contracts in a selected source marketplace
argument-hint: "[plugin-name] [--root /path/to/checkout]"
---

Read `../skills/marketplace-manager/SKILL.md` relative to this command. Resolve the selected source checkout and run its packaged manager's `scan` action. An installed plugin cache is not the marketplace source root. Component manifest paths resolve from the plugin root, not `.claude-plugin/`.

User request: $ARGUMENTS
