---
description: Create or modify an editable Excalidraw diagram
argument-hint: <description or existing scene path>
---

Use the workflow in `../skills/excalidraw/SKILL.md`, resolved relative to this command file, for the user's request:

$ARGUMENTS

Read an existing scene before edits. Resolve the converter relative to the plugin, verify its exit status, and return the resulting file path.
