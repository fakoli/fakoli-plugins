.PHONY: install test lint clean

install:
	@echo "no dependencies (bash + git only)"

test:
	bash tests/test-gate-router.sh

lint:
	bash -n scripts/gate-router.sh tests/test-gate-router.sh

clean:
	@echo "nothing to clean"
