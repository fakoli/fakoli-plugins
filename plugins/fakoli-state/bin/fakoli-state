#!/usr/bin/env bash
# Preserve the caller's cwd; the shared launcher keeps dependencies out of the install cache.
BIN_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)" || exit 1
exec bash "$BIN_DIR/launch.sh" fakoli_state.cli "$@"
